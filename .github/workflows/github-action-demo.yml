name: GitHub Actions Demo yipee
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches:
      - main
jobs:
  determine-changes:
    runs-on: ubuntu-latest
    outputs:
      HAS_APP_CHANGES: ${{ steps.changes.outputs.app }}
      HAS_BFF_CHANGES: ${{ steps.changes.outputs.bff }}
    steps:
      - uses: actions/checkout@v4
      - name: Determine new changes
        uses: dorny/paths-filter@v2
        id: changes
        with:
          filters: |
            bff:
              - 'bff/**'
            app:
              - 'app/**'
  code_analysis:
    name: Analyze code
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'javascript' ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: 'security-and-quality'
          name: JavaScript CodeQL Configuration

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{matrix.language}}"
  appBuild:
    needs: [ determine-changes ]
    if: ${{ needs.determine-changes.outputs.HAS_APP_CHANGES == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo “🎉 APP build successful for ${{ github.event_name }} event.”
  bffBuild:
    needs: [ determine-changes ]
    if: ${{ needs.determine-changes.outputs.HAS_BFF_CHANGES == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo “🎉 BFF build successful for ${{ github.event_name }} event.”
  app-deploy-without-bff:
    needs: [determine-changes, appBuild]
    if: (needs.determine-changes.outputs.HAS_BFF_CHANGES == 'false')
    runs-on: ubuntu-latest
    steps:
      - run: echo “🎉 App deployment without bff successful for ${{ github.event_name }}.
  app-deploy:
    needs: [ determine-changes, appBuild, bffBuild ]
    if: (needs.appBuild.result == 'success' && needs.bffBuild.result == 'success')
    runs-on: ubuntu-latest
    steps:
      - run: echo “🎉 App deployment with bff successful for ${{ github.event_name }}.
  bff-deploy:
    needs: [ determine-changes, appBuild, bffBuild ]
    if: (needs.appBuild.result == 'success' && needs.bffBuild.result == 'success')
    runs-on: ubuntu-latest
    steps:
      - run: echo “🎉 BFF deployment with bff successful for ${{ github.event_name }}.
  bff-deploy-without-app:
    needs: [determine-changes, bffBuild]
    if: (needs.determine-changes.outputs.HAS_APP_CHANGES == 'false')
    runs-on: ubuntu-latest
    steps:
      - run: echo “🎉 BFF deployment without app successful for ${{ github.event_name }}.”
